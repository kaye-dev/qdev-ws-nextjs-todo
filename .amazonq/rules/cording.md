# cording.md

## 目的

プロジェクトにおけるコーディングスタイルとベストプラクティスを定義し、コードの一貫性、可読性、保守性を向上させることを目的としています。

## コーディングスタイル

- インデントはスペース 2 つを使用します。タブは使用しません
- 変数名、関数名、クラス名はキャメルケースを使用します（例: `myVariable`, `myFunction`, `MyClass`）
- 定数はすべて大文字でアンダースコアで区切ります
  - （例: `MAX_VALUE`, `DEFAULT_TIMEOUT`）
- 行の長さは 120 文字以内に制限します
- コメントはコードの意図を説明するために使用し、冗長なコメントは避けます
- 関数は単一の責任を持つように設計し、長すぎる関数は避けます
- クラスは単一の責任を持つように設計し、過度に複雑なクラスは避けます
- エラーハンドリングは適切に行い、例外をキャッチしてログに記録します
- 不要なコードやコメントアウトされたコードは削除します
- 一貫した命名規則を使用し、プロジェクト全体で統一します
- 使用していない変数や定数、関数があれば適宜削除します
- コメントアウトは日本語でします
- ファイル保存時に ESLint でチェックを行い、違反があれば修正します
- `npm run build` を実行し、ビルドエラーが無いことを確認します
- cdk の実装をした場合、`cdk:synth` で出力テンプレートを確認すること

## プロジェクト固有のルール

### Next.js App Router 特有

- Server Components と Client Components の区別を明確にする
- 'use client'ディレクティブは必要な場合のみ使用し、ファイル先頭に配置
- API Routes は`src/app/api/`配下に配置
- ページコンポーネントは`src/app/`配下に配置

### TypeScript 型定義

- `any`型の使用は避け、適切な型定義を行う
- インターフェース定義は`src/types/`に集約
- 共通型は`src/types/index.ts`でエクスポート
- コンポーネントの props は明示的にインターフェースで定義

### AWS CDK 固有

- スタック名には必ず`cdk.json`の`name`を使用
- リソース名は`${name}-${resourceType}`の形式で命名
- 環境変数は`process.env`で明示的に取得
- デプロイ前に必ず`npm run cdk:synth`で確認

### コンポーネント設計

- 1 ファイル 1 コンポーネントの原則
- props は明示的にインターフェースで定義
- ビジネスロジックはカスタムフック(`src/hooks/`)に分離
- 再利用可能なコンポーネントは`src/components/`に配置

### 環境別設定

- `process.env.NODE_ENV`を使用して開発環境と本番環境で動作を切り替える
- 開発環境専用の設定は`next.config.ts`で条件分岐する
  - 例: `...(process.env.NODE_ENV === "development" && { basePath: "/proxy/3000" })`
- API呼び出しパスは環境変数で切り替え可能にする
  - 開発環境: `/proxy/3000/api/*`
  - 本番環境: `/api/*`
- 環境依存の定数はファイル先頭で定義する
  - 例: `const API_BASE = process.env.NODE_ENV === "development" ? "/proxy/3000" : "";`

### DynamoDB 操作

- DynamoDB クライアントの初期化は`src/utils/dynamodb.ts`を使用
- テーブル名は環境変数から取得
- エラーハンドリングを適切に実装

## ワークショップ特有の制約

### 必須確認事項

- `cdk.json`の`name`は必ず一意の値に設定すること
- 他の参加者と名前が重複しないよう英数字+ハイフンで命名
  - 推奨形式: `参加者名-識別子`（例: `taro-yamada`, `workshop-01`）
- デプロイ前に必ず`npm run cdk:synth`でテンプレート確認
- デプロイ前に必ず`npm run build`でビルドエラーがないことを確認

### リソース制限

- VPC の新規作成には制限があるため、共有 VPC の利用を推奨
- スタック名の重複を避けるため、削除時は完全に削除されるまで待機

## 依頼したタスク完了後

依頼したタスクの実装が完了した場合、日本語のワンライナー `git commit` メッセージを考えてください。
